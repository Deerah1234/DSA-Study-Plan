#include <stdio.h>

// The structure type of a singly linked list type
/*
	data: contains the date of the node.
	next: contains the pointer to the next node.
*/
struct node
{
	int data;
	struct node *next;
};